# Kokedera for kitty

Copy the desired `.conf` into `~/.config/kitty/themes/`. Add or update the following line in your existing `kitty.conf`:

```conf
include themes/kokedera-dusk.conf
```

Reload kitty’s configuration. Later color settings in your configuration may override the included theme. The files contain colors only.

## Variants

Kokedera Dusk (dark), Kokedera Morning (light), Kokedera Night (dark), Kokedera Spring (dark), Kokedera Summer (dark), Kokedera Autumn (dark), Kokedera Winter (light), Kokedera Rain (dark), Kokedera Mist (dark).

All formats use the same shared 16-color ANSI palette, foreground and background for each variant. Application-specific cursor, selection and UI behavior may differ.

## References

- [kitty documentation](https://sw.kovidgoyal.net/kitty/conf/)
- [Format reference](https://sw.kovidgoyal.net/kitty/kittens/themes/)
